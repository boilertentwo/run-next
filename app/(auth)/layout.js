


export const metadata={
    title: 'Login | orderBook',
    description: 'Register to access services.'
}

export default function LoginLayout({children}){

  
    return(
        <>  
            
                <main>
                    {children}
                </main>
            
        </>
    )
}